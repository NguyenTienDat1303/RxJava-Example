<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rxjavademo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OperatorMenuActivity"></activity>
        <activity android:name=".SkipLastActivity" />
        <activity android:name=".SkipActivity" />
        <activity android:name=".DistinctActivity" />
        <activity android:name=".FilterActivity" />
        <activity android:name=".BufferActivity" />
        <activity android:name=".ConcatMapActivity" />
        <activity android:name=".FlatMapActivity" />
        <activity android:name=".RangeActivity" />
        <activity android:name=".FromArrayActivity" />
        <activity android:name=".BasicActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".CreateActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>